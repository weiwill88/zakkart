# 工程日志：客户反馈修复与测试补齐

> 反馈来源：`erp系统PC&App端反馈20260422.docx`
> 执行日期：2026-04-30

## 11:00 左右｜反馈拆解与影响范围确认

- 将反馈归为四类：配件管理、合同生成/上传、司机 H5、小程序供应商/发货/库存。
- 已确认本轮不新增数据库集合，继续复用 `organizations / contracts / part_types / shipment_orders / users / inventory` 等现有集合。
- 当前工作区原本已有多处未提交改动，本轮按增量修改处理，没有回滚既有文件。

## 11:05 左右｜后端修复

- 修复配件管理批量编辑的唯一性口径：唯一性指纹从“类别 + 名称 + 供应商”扩展到“类别 + 名称 + 供应商 + 材质 + 颜色 + 尺寸”，避免同名但规格不同的配件被误判重复。
- 合同付款字段调整为：`supplier_bank_account_name` 表示户名，默认供应商公司名称；`supplier_bank_name` 表示开户银行；兼容旧合同字段。
- 合同交付计划增加强校验：每个采购货物的交付计划合计必须等于采购数量；一次性费用行不参与交付计划。
- 司机 H5 `ensureDriverObject` 改为只做内存归一化，避免对云数据库写入空对象导致 `$set is empty`。
- `order.generate` 生成合同时读取供应商 `contract_template.quality_clause`，支持按公司定制“成品货物要求”。

## 11:09 左右｜PC 端修复

- 合同正文中“采购数量”改为只录合计数量，不再展示 SKU 明细文本。
- 合同采购结算主表增加“一次性费用”行，费用计入总金额但不进入交付计划。
- 合同编辑、保存、推送确认、导出 Word、上传已签 PDF 前都接入交付数量一致性校验。
- 合同支持上传附录图纸；在线合同可预览，Word 导出会尽量嵌入可访问图片，无法嵌入时保留文件标识。
- 供应商银行信息主流程移除“开户行全称”输入；新增供应商合同模板字段，当前先支持“成品货物要求”。
- PC 云存储上传前强制确认匿名登录，并将 CloudBase storage 失败提示改得更明确。
- 库存新增表单默认进入“成品”库存，新增时不再展示在制品/半成品输入。

## 11:13 左右｜小程序与 H5 修复

- 供应商管理列表改为展示真实 `cooperation_status`，已停止合作的工厂不再显示“合作中”。
- 供应商成员无合同权限时，首页不再主动请求合同列表；后端也会阻断无合同权限的合同列表/详情/PDF 访问。
- 发货创建页对地址数据做 `id` 归一化，避免模板层直接依赖 `_id` 并减少 `document.get` 类报错扩散。
- 包装厂出库页补齐 `bindinput` 与表单校验，修复出库数量、目的地无法录入的问题；本轮不扩展完整阶段 5 出库后端闭环。

## 11:16 左右｜测试补齐

- 新增云函数测试：合同付款户名、交付数量校验、配件唯一性指纹、H5 空更新防护。
- 新增 PC 测试：合同 payload 归一化、一次性费用、交付计划不一致拦截、Word 生成、库存默认表单。
- 新增小程序纯逻辑测试：供应商状态展示、发货地址归一化、目的地规则、出库表单校验。
- 新增统一测试脚本：`scripts/run-unit-tests.sh`。

## 11:18 左右｜验证结果

- `find cloudfunctions/api -type f -name '*.js' -maxdepth 3 -print | sort | xargs -n 1 node -c`：通过。
- `cd cloudfunctions/api && npm test`：4 个测试通过。
- `cd admin && npm test`：5 个测试通过。
- `node --test "miniprogram/tests/**/*.test.js"`：4 个测试通过。
- `cd admin && npm run build`：通过；仍保留 Vite 大 chunk 警告，非本轮阻断项。
- 已重新打包 `deploy-packages/api-cloudfunction.zip`，但云端部署仍建议按既定流程用微信开发者工具 GUI 执行。

## 明日验收建议

- PC：配件管理批量保存、供应商银行信息、合同付款户名、合同交付数量不一致提示、一次性费用、附录图纸上传和 Word 导出。
- 小程序：供应商管理停止状态展示、无合同权限成员看不到合同、工厂创建发货单地址选择、包装厂出库数量/目的地输入。
- H5：司机扫码 Step 1 接单登记不再出现 `$set is empty` 报错。

## 13:10 左右｜正式域名与轻量服务器配置

- 腾讯云 DNSPod 已为 `zakkart.cn` 增加/确认两条正式解析：`erp.zakkart.cn -> 101.35.223.110`、`driver.zakkart.cn -> 101.35.223.110`，本机 `dig +short` 均返回 `101.35.223.110`。
- 腾讯轻量服务器防火墙已放通 `80` 和 `443`；原有 SSH、Ping 规则保留。
- 服务器内已安装 Nginx，并将本地构建包 `/tmp/zakkart-static.tar.gz` 上传到 `/root/zakkart-static.tar.gz` 后解压部署：
  - PC 管理端：`/var/www/zakkart/admin/current`
  - 司机 H5：`/var/www/zakkart/driver/current`
- Nginx 已配置双站点：`erp.zakkart.cn` 指向 PC 管理端，`driver.zakkart.cn` 指向司机 H5；PC 端使用 `try_files ... /index.html` 支持前端路由。
- 已通过 Certbot 为 `erp.zakkart.cn` 和 `driver.zakkart.cn` 签发 Let's Encrypt 证书并开启 HTTP 自动跳转 HTTPS；证书有效期至 `2026-07-29`，`certbot-renew.timer` 已启用用于自动续期。
- 验证结果：
  - `curl -I http://erp.zakkart.cn`：`301` 跳转到 `https://erp.zakkart.cn/`
  - `curl -I https://erp.zakkart.cn`：`200 OK`
  - `curl -I http://driver.zakkart.cn`：`301` 跳转到 `https://driver.zakkart.cn/`
  - `curl -I https://driver.zakkart.cn`：`200 OK`
- CloudBase 账号与腾讯云账号不同，本轮暂未配置 CloudBase；待切换到 CloudBase 后继续添加 Web 安全域名 `erp.zakkart.cn`、`driver.zakkart.cn`，并更新云函数环境变量 `H5_BASE_URL=https://driver.zakkart.cn/`。

## 13:41 左右｜CloudBase 正式域名配置

- CloudBase 环境确认仍为 `cloud1-0g855x4wc43be3c4`，本次只改正式域名相关配置，未删除既有 `localhost`、Vercel、CloudBase 默认域名。
- 已在“环境管理 -> 跨域设置 / WEB 安全域名”中新增：
  - `erp.zakkart.cn`
  - `driver.zakkart.cn`
- 已确认“身份认证 -> 登录方式”中的匿名登录开关处于开启状态，符合 PC/H5 端使用 CloudBase Web SDK 匿名登录调用云函数的方案。
- 已在 `api` 云函数配置中将 `H5_BASE_URL` 更新为 `https://driver.zakkart.cn/`，保存后函数状态恢复为“正常”，配置修改时间显示为 `2026-04-30 13:41:08`。
- CloudBase 页面提示 Web 安全域名配置可能存在短暂生效延迟；正式验收时如刚配置完仍出现安全域名类错误，先等待约 10 分钟后再复测。
- 下一步验收重点：PC 端从 `https://erp.zakkart.cn` 调用 CloudBase 不再被安全域名拦截；新建发货单生成的司机链接应使用 `https://driver.zakkart.cn/?token=...`。

## 13:49 左右｜短信登录切换准备

- 确认线上登录页仍显示“开发期验证码固定为 `123456`”，根因是 `auth.smsSend` / `auth.smsLogin` 后端仍走开发期 mock 逻辑，短信 Key 已配置但代码尚未调用腾讯云短信。
- 已新增腾讯云短信发送工具，使用现有环境变量 `SMS_SECRET_ID / SMS_SECRET_KEY / SMS_SDK_APP_ID / SMS_SIGN_NAME / SMS_TEMPLATE_ID`，不新增第三方依赖。
- 验证码改为后端生成 6 位随机码，通过腾讯云短信发送后，仅将哈希、过期时间、请求时间和错误次数写入对应 `users` 文档；登录成功后清空验证码状态。
- 保留安全回退：只有显式配置 `SMS_MOCK=true` 或短信环境变量缺失时，才继续使用开发期固定验证码。
- PC 登录页已移除固定验证码文案，改为正式短信验证码登录说明。
- 已重新打包 `deploy-packages/api-cloudfunction.zip`，其中包含真实短信逻辑；正式激活还需要将该 ZIP 通过 CloudBase GUI 重部署到 `api` 云函数，并将最新 `admin/dist` 同步到轻量服务器。
- 验证结果：
  - `./scripts/run-unit-tests.sh`：通过，当前云函数 6 个测试、PC 5 个测试、小程序 4 个测试均通过。
  - `cd admin && npm run build`：通过；仍保留 Vite 大 chunk 警告，非短信功能阻断项。

## 14:35 左右｜短信登录线上激活

- 已将包含真实短信逻辑的 `deploy-packages/api-cloudfunction.zip` 通过 CloudBase GUI 上传到 `api` 云函数。
- 第一次仅点击“保存”后出现 `Cannot find module 'wx-server-sdk'`，判断为依赖未安装；随后重新使用同一 ZIP 执行“保存并安装依赖”，函数恢复正常。
- 已通过 PC 端同源 CloudBase Web SDK 调用 `system.health` 验证线上状态：
  - `features.sms_login = true`
  - `features.sms_mock = false`
- 结论：CloudBase 后端已经从开发期固定验证码切到腾讯云短信真实发送模式；不再依赖 `123456`。
- 当前遗留：`https://erp.zakkart.cn` 的 PC 静态包仍是旧版本，登录页 JS 中仍包含“开发阶段固定为 123456”的文案。该文案不影响后端真实短信发送，但会误导使用者，仍需覆盖轻量服务器 `/var/www/zakkart/admin/current`。
- 覆盖静态包时遇到的权限/账号阻塞：
  - 本机 SSH 到 `root@101.35.223.110` 返回 `Permission denied`，无法直接 `scp`。
  - 当前腾讯云控制台账号下，顶部资源搜索 `101.35.223.110` 显示云资源为 0；OrcaTerm 当前也没有可用连接配置。
  - 需要重新打开拥有该轻量服务器的腾讯云账号，或直接打开已登录的 root 终端，再同步 `/tmp/zakkart-admin-dist-20260430-sms.tar.gz` 到服务器并切换 Nginx 静态目录。

## 15:10 左右｜PC 静态包覆盖完成

- 已将最新 PC 构建包固化到仓库部署包：`deploy-packages/zakkart-admin-dist-20260430-sms.tar.gz`，并推送到 `origin/main`。
- 服务器直接从 GitHub raw 下载出现 SSL 连接超时，因此改为先上传同一部署包到 CloudBase 云存储，再由服务器通过临时链接下载。
- 服务器下载后 SHA256 校验通过：`1c4180981247ca29904bd5703e1d45335beafdb6e8d1c1199817ff3234c34b3d`。
- 新 release 目录：`/var/www/zakkart/admin/releases/20260430-sms`；`/var/www/zakkart/admin/current` 已切换到该目录。
- `nginx -t` 通过，`systemctl reload nginx` 已执行；司机 H5 目录未修改。
- 线上验证结果：
  - `curl -I https://erp.zakkart.cn`：`200 OK`。
  - `https://erp.zakkart.cn` 当前引用 `index-D8HIjn3_.js` 与 `LoginView-CeJGa8_G.js`。
  - 线上登录 JS 已不再包含 `123456`、`Phase 0`、`开发阶段固定` 等旧开发文案，显示正式短信验证码登录说明。
